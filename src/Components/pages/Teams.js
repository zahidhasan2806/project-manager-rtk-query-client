import React from 'react';
import Layout from '../ui/Layout';

const Teams = () => {
    return (
        <div>
            <Layout>

            </Layout>
        </div>
    );
};

export default Teams;